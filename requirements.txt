# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
pydantic-settings>=2.0

# PostgreSQL + PostGIS
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29.0
geoalchemy2>=0.15.0

# Kafka
confluent-kafka>=2.3.0

# Valkey (Redis-compatible client)
redis>=5.0.0

# MySQL (async)
aiomysql>=0.2.0

# Sync PostgreSQL (for consumer + scripts)
psycopg2-binary>=2.9.0

# ETL / Utilities
pandas>=2.0
shapely>=2.0
certifi>=2024.0

# Development
httpx>=0.27.0  # async test client for FastAPI
